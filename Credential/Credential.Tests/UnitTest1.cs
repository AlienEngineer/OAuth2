using System;
using Machine.Specifications;

namespace Credential.Tests
{
    [Subject(typeof (UriBuilder))]
    public class When_uri
    {
        Establish context = () =>
        {
            
        };

        private Because of = () => { };

        private It should_ = () => { };
    }
}
